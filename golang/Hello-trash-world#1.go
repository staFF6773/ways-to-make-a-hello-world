package main

import (
    "fmt"
)

func main() {
    var h, e, l1, l2, o, comma, space, w, r, d string
    h = "H"
    e = "e"
    l1 = "l"
    l2 = "l"
    o = "o"
    comma = ","
    space = " "
    w = "W"
    r = "r"
    d = "d"

    fmt.Print(h)
    fmt.Print(e)
    fmt.Print(l1)
    fmt.Print(l2)
    fmt.Print(o)
    fmt.Print(comma)
    fmt.Print(space)
    fmt.Print(w)
    fmt.Print(o)
    fmt.Print(r)
    fmt.Print(l1)
    fmt.Print(l2)
    fmt.Println(d)
}
